#ifndef DATATYPE_H
#define DATATYPE_H

typedef enum {
    TYPE_UNKNOWN,       // (по умолчанию)
    TYPE_BOOL,
    TYPE_CHAR,
    TYPE_SHORT,
    TYPE_INT,
    TYPE_LONG,
    TYPE_LLONG,
    TYPE_FLOAT,
    TYPE_DOUBLE
} DataType;

#endif // DATATYPE_H
